import React from 'react';
import { Icon } from './ArrayVisualizer';

type ArrayIconProps = {
	ball: Icon;
};

export default function ArrayIcon({ ball }: ArrayIconProps) {
	return <div>ArrayIcon</div>;
}
